/**
 * Speed layer is now integrated into RoadLayer via feature-state coloring.
 * This component is kept as a no-op for backward compatibility.
 */
export function SpeedLayer(_props: { osmId: string }) {
	return null
}
